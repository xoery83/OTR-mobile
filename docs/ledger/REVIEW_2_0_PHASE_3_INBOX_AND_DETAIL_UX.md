# Review 2.0 Phase 3 — Inbox and Finding Detail UX

Status: implementation in progress (2026-09-17). Phase 1/2 Hosted Dev acceptance is the baseline; no schema, eligibility, rule, or Backend changes are planned. Production is out of scope.

## Implementation and acceptance record

- Implemented `reviewInbox` active personal/category projection, virtualized OPEN/Reviewed sections, collapsed Reviewed default, a small separately collapsed inactive-v2 History section, immutable card context, and five rule-specific detail evidence layouts. Existing Review navigation remains available at zero pending.
- Detail submission locks both buttons until its local transaction finishes; selected action remains disabled. The repository's same-decision/no-reason no-op from Phase 2 stays intact. A post-transaction repository notification reloads mounted Review/Ledger projections before navigation return, with focus reload as a safety net. Ledger banner now uses `review.counts(...).pending`, not a mixed list-status count.
- Account-switch, 403 removal, durable offline action, resolution/new-generation, and pending count behavior continue to rely on the Phase 2 repository/sync contract. TypeScript, ESLint, 74 Vitest files / 271 tests, and `git diff --check` pass. Focused projection/evidence and SQLite post-commit notification tests supplement the existing personal-decision/offline/duplicate-action tests. There is no installed React Native UI test renderer; complete accessibility interaction and end-to-end device acceptance remain open.
- Signed Release was installed on the iPhone 17 Pro Simulator using the existing real Dev Owner session. Synthetic Baseline Journey showed 17 pending before the controlled ACK; Duplicate filter showed 5, Participants 12, Reviewed 2 collapsed. Expanding Reviewed showed two Owner ACKs. A Duplicate detail showed both NZD 20.00 observations, payer and both Expense links; ACK changed its personal state and OPEN fell to 16 total/4 Duplicate while Reviewed rose to 3. Ledger banner fell to 16. This first run exposed a transient stale list frame on return. After the post-commit notification fix, a fresh signed Release was installed on the same simulator: a second Duplicate was dismissed, and **the first returned frame** showed 15 total/3 Duplicate in OPEN and Reviewed 4, with the dismissed row absent. Participants detail displayed the payer and participant names. This verifies the stale-return fix on simulator. Current Owner sample has zero Amount, Exchange rate and Evidence pending items, so those three details were not visually exercised. Offline, same-device account switch, Expense resolution and zero-banner acceptance also remain open.
- The signed Release device build succeeded, was installed and launched on the connected iPhone 16 Pro (iOS 26.6). Device Hub cannot screen-share below iOS 27, but the user unlocked iPhone Mirroring; physical UI was then observed. Using the real remembered Dev Owner and Synthetic Baseline Journey, Ledger showed 15 pending, the Review page showed 15 total/3 Duplicate, Reviewed 4 collapsed, and the Duplicate detail showed both €100.00 Expense observations, payer and links. One physical ACK immediately changed detail to Acknowledged; the first return frame showed 14 total/2 Duplicate and Reviewed 5, and Ledger banner showed 14. The physical test-account selector states no other account is remembered on this phone. The physical offline ACK, account-switch, zero-banner and three categories without current samples remain **unaccepted**. Hosted Dev Phase 3 UI acceptance is partial; no Production access/deployment. Do not mark complete until the open checks pass.

## Current UI and target information architecture

The existing Review route lists all visible Findings together, including historical and personally reviewed items. Its generic cards show live Expense titles and rule prose, not immutable observation evidence. List and detail each own a hook instance; acting in detail does not invalidate the list. Ledger's attention banner counts both OPEN and ACKNOWLEDGED. The selected decision is disabled but a rapid opposite tap can still race.

Review becomes a financial attention inbox: a total personal pending headline; horizontal All, Amount, Duplicate, Exchange rate, Participants, Evidence chips with _pending_ counts; OPEN with only visible active v2 heuristic NEEDS_REVIEW items; a secondary `Reviewed N` accordion, collapsed on entry, containing only active personal ACKNOWLEDGED/DISMISSED items. Both lists obey the selected category; the headline and chip counts do not. Resolved, superseded, deterministic, and v1 items stay out of this active inbox. Existing Ledger Review navigation remains available at zero pending; a minimal history affordance may show eligible inactive rows separately, never inside OPEN/Reviewed. Future USER_REQUEST is an extension, not a button/schema in this phase.

## Presentation and interactions

Cards: category, human title, immutable Expense title/date and useful payer where available, one-line rule-specific numerical or identity evidence, personal status in Reviewed, chevron. Stable Finding ID keys and virtualized list rendering; parse evidence once in the repository. Never expose rule IDs/versions or invent market-rate/OCR comparisons.

Detail uses the saved observation context, not today's mutable Expense values:

| Category      | Observation and comparison                                                                                 | Next step                                   |
| ------------- | ---------------------------------------------------------------------------------------------------------- | ------------------------------------------- |
| Amount        | Original amount, same-currency/scale Journey median, cohort size, multiplier                               | Open affected Expense                       |
| Duplicate     | Both saved Expense titles, dates, payer identities and amounts; matched Expense ID                         | Open either Expense                         |
| Exchange rate | Recorded rate, source/currency pair when known, actual bound `> 0` and `<= 1,000`, direction               | Open affected Expense; no market-rate claim |
| Evidence      | Original amount, selected posted payment amount and absolute difference; payment record/date only if saved | Open affected Expense; not receipt OCR      |
| Participants  | Payer and saved participant names (fallback to identities if unavailable), neutral explanation             | Open affected Expense                       |

Active eligible detail offers Acknowledge and Dismiss without required reason or confirmation. A local transaction persists personal decision and one durable operation before UI reports success; disable both controls during submission and the selected control thereafter. The Reviewed detail shows personal status, and existing supported change-decision action may remain. The UI says a queued action is waiting to sync, never blocks offline. A later server projection may remove an ineligible, resolved, or superseded Finding; a new generation has its own pending identity.

## Reactive state, counts and navigation

One user-scoped repository projection supplies the active inbox and canonical counts. Notify interested screens after local decision, projection replacement, and invalidation; reload on navigation focus as a safety net for Expense edits/account switches. The Ledger banner uses repository pending count and disappears at zero with no reserved space or `Review 0`. Account identity is rechecked by the repository on every read. The list/detail reload on focus rather than depending on remount. Remote refresh follows immediate cached load; failure retains cache and displays a subtle offline/error message. No full-screen spinner when cache exists. Initial cache absence gets lightweight loading; zero pending says “Nothing needs review,” while Reviewed remains reachable.

## Accessibility and acceptance

Chips have category/count labels and selected states; action buttons expose disabled/selected state; accordion exposes expanded state; statuses are words, not just color. Rows/buttons meet 44–48pt targets and text scales without fixed-height clipping. Pending count is announced. Verify empty, loading, error and offline states, multi-account isolation, category counts/filtering, reviewed default collapse, both actions/duplicate taps, focus return, banner zero, all five immutable detail layouts, resolution/new generation, and accessible labels/states in automated checks.

After tests/build, perform Hosted Dev UI acceptance with real Owner/Member Auth: independent decisions, immediate ACK, categories, accordion, all five details, offline ACK, Expense-edit resolution, zero banner, same-device switch. Check a simulator and physical iPhone where available; record the exact observed device/screenshots and any unavailable visual checks. Phase 3 must not be marked complete before Hosted Dev and physical UI acceptance.
