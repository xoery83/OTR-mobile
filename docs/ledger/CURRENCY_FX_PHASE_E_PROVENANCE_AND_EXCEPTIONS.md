# Currency / FX Phase E — provenance and exception UX

Status: **PASS WITH DEVICE ACCEPTANCE PENDING** (Simulator/Hosted Dev closure 2026-09-18). No Production change or Phase F.

## Product language and read model

The Expense detail shows original Money and Journey value first. Cross-currency accepted reference evidence is labelled **Reference rate / 参考汇率**, never actual/bank/transaction rate. `MANUAL_AGREED` is **Agreed rate / 约定汇率** and `ACTUAL_PAYER_COST` is **Actual payer cost / 实际付款金额**. Same-currency Expenses show the identity Journey amount without an FX policy. Missing economic date requests date confirmation; a known date without accepted valuation says the reference rate is pending. A locally queued valuation is explicitly pending sync.

Rate details discloses the canonical original→Journey rate, both dates when available, reason and the accepted reference provenance already cached in the existing SQLite valuation snapshot. When dates differ, the Expense date and actual ECB reference publication date are separate. The European Central Bank is the financial reference source; Frankfurter delivers that source's data. The detail clarifies that this is valuation information, not card/bank cost. No internal IDs or provider payloads appear. A short list of previously cached inactive valuation snapshots explains currency/policy supersession without creating a parallel history store; a newly bootstrapped device does not currently fetch old snapshots, so this is explicitly labelled “on this device.” The canonical server retains all historical rows. No schema or Backend read-contract migration was made.

## Explicit policy changes

Only the linked creator or owner can change an Expense valuation under the Stage 5 server RPC. The broader actor capability flag is not sufficient for another group member. Owner changes to another creator's Expense require a reason at the server; reference restoration supplies one. Detail controls are suppressed for conflict, deleted/pending Expense, and an Expense included in locally cached finalized settlement inputs; the server repeats the revision, membership, quote, posted-payment and finalized-input guards. Other authorized members may read provenance. If local finalized inputs have not yet been pulled, the server remains authoritative and rejects the change.

Agreed rate accepts a positive exact decimal in one direction (`1 original = X Journey`) and a mandatory reason. It previews the exact integer-minor-unit Journey Money using the existing domain valuation function before explicit confirmation. Actual payer cost offers only non-superseded posted PaymentRecords in Journey Currency/scale, previews the posted amount, and explicitly confirms; it never derives a reusable market rate. A synced eligible ECB candidate allows an explicit preview and return to Reference rate. No quote refresh switches an accepted valuation by itself. All three actions use the existing local-first `LEDGER_APPLY_VALUATION` operation, revision/audit and immutable superseding Stage 5 path. The worker maps local payer evidence ID to its canonical server ID and waits until that evidence syncs. Pending operations are not represented as remotely accepted.

Current reports and Review are unchanged consumers of the one active canonical valuation. Stage 5 writes one revised active snapshot and settlement splits, retaining previous evidence; Review re-evaluation follows canonical sync, and settlement readiness reads the active value. Finalized inputs remain frozen. Cached provenance and prior local evidence remain readable without network. The standalone Exchange Rates placeholder is hidden from ordinary Settings navigation; no rate-cache administration is exposed. The route remains inert for old deep links.

## Verification and limitations

Focused tests cover quote direction/source/date/expiry, unresolved copy in English and Simplified Chinese, exact manual preview/reason, posted-cost semantics, canonical payment-ID transport, and the local finalized-input check; existing Stage 5 and Phase C/D tests cover supersession, Review/reporting, and finalized guards. At the source-implementation checkpoint, TypeScript, ESLint, Backend build and 80 Mobile files/316 tests passed; local migration reset and 16 pgTAP files/301 checks passed. Full-repository formatting flagged only the pre-existing unrelated `AGENTS.md` and `src/hooks/useStage4BPhysicalSmoke.ts`; Expo Doctor was unavailable locally. A signed iPhone 17 Pro Simulator showed the Phase D QA same-currency EUR100 identity and ISK10,000→EUR69.60 weekend case, separate 12/10 July dates, ECB versus Frankfurter attribution and prior ISK→NZD valuation. The final summary layout was rebuilt and installed over the app without clearing data. A new device still receives only the active valuation in bootstrap; full server-side chronological history is not projected to Mobile. UI/server policy does not attempt automatic policy precedence.

## Acceptance closure — 2026-09-18

Acceptance matrix: `MANUAL_AGREED` Commit **PASS**; restore `REFERENCE_RATE` **PASS**; `ACTUAL_PAYER_COST` Commit **PASS**; reporting **PASS**; Review/history **PASS**; non-finalized settlement preview **PASS**; finalized valuation mutation guard **PASS**; restart/idempotency **PASS**; true offline cold-start interaction **PENDING**; physical iPhone interaction **PENDING**.

- Signed iPhone 17 Pro Simulator, normal UI and Hosted Dev `tuqigdxrvrerfewsxqgm`: the disposable 1 ISK Expense `be608647-6ac7-4520-8117-dfeb212fb91d` advanced from accepted ECB reference €0.01/revision 2 to agreed rate `1001`/€1,001.00/revision 3, explicitly back to reference €0.01/revision 4, then to posted payer cost €1.25/revision 5. Original 1 ISK never changed. Four immutable valuation rows form the correct supersession chain, exactly one active; the manual reason remains historical. Reference restoration uses the same eligible B2 quote (`2026-07-12` Expense date, `2026-07-10` ECB reference date, delivered via Frankfurter). Actual cost links separate posted PaymentRecord `a9e8fb8d-31da-4c17-9eea-849401ed82e1` and has no market-rate snapshot or decimal rate. Three UI commands have one successful idempotency receipt, audit event and change-feed revision each. Force quit/restart retained the active cost without duplicate evidence.
- Mine, Group, Category and Analysis category/payer/participant converged to €220.10 → €1,221.09 → €220.10 → €221.34 as the active valuation changed; four Expenses remained counted once. The immediate post-commit view briefly mixed an old summary with a new category amount, then converged on refresh. The manual `1001` rate generated one `RATE_OUTLIER` Review finding for revision 3. Its personal ACK and immutable action remained after reference/actual transitions made that generation `STALE`; no duplicate active finding remained. Settlement previews were ready with no transfers/blockers, and canonical input digests changed from `387387a4…` (reference) to `d1940d88…` (manual) to `935c7e1f…` (actual). No final settlement was created; the existing stale-preview pgTAP covers finalization rejection.
- A Dev-only finalized cross-currency Expense (`310f467f-7ade-529b-a85c-1151451102ea`, DKK 75.00 → CNY 18.50) displayed its agreed-rate reason and a finalized lock, with no valuation-changing controls. A direct rejection probe exposed that the Stage 7.2B migration had redefined the Stage 5 function without its finalized-input guard. Forward migrations `20260918000100` and `20260918000200` restore the guard at the existing RPC boundary while preserving completed-command idempotent replay; neither changes financial data or the API. Hosted Dev now returns `FINALIZED_SETTLEMENT_PROTECTED`; stale revision and unauthorized probes return `REVISION_CONFLICT` and `TRIP_WRITE_FORBIDDEN`, and an earlier accepted valuation replays without a new write. Frozen input, revision and active valuation remained unchanged. Local reset and all 16 pgTAP files/303 checks pass; Dev dry-run reports no pending migrations.
- Offline cold-start acceptance remains pending: this Simulator exposes neither a usable Wi-Fi/airplane switch in Settings nor a connectivity control, so network isolation could not be proven. Physical iOS 26.6 interaction remains pending because Device Hub cannot control that OS version. QA financial evidence remains append-only; no cleanup bypass was used. Production is untouched.
